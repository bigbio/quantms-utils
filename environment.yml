# environment.yml
name: quantmsutils
channels:
  - defaults
  - bioconda
  - conda-forge
dependencies:
  - click
  - sdrf-pipelines
  - bioconda::pyopenms
  - ms2rescore=3.0.2
  - psm-utils=0.7.4
  - pydantic
  - pandas
  - protobuf>=3.9.2,<4  # Ensure compatibility with tensorboard
  - numpy
  - pip
  - pip:
    - tensorboard
