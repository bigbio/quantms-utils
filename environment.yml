# environment.yml
name: quantmsutils
channels:
  - bioconda
  - conda-forge
  - nodefaults
dependencies:
  - click
  - sdrf-pipelines>=0.0.31
  - pyopenms
  - pandas
  - numpy
  - pyarrow>=18.0.0
  - scipy