# environment.yml
name: quantmsutils
channels:
  - defaults
  - bioconda
  - conda-forge
dependencies:
  - click
  - sdrf-pipelines>=0.0.29
  - pyopenms
  - ms2rescore=3.0.2
  - psm-utils=0.8.0
  - pydantic
  - pandas
  - protobuf>=3.9.2,<4  # Ensure compatibility with tensorboard
  - numpy
  - pyarrow
  - pip
  - pip:
    - tensorboard
