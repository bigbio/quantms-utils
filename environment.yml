# environment.yml
name: quantmsutils
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - click
  - sdrf-pipelines>=0.0.31
  - pyopenms
  - pandas
  - numpy
  - pyarrow
  - pip
  - pip
     - ms2rescore=3.1.3
